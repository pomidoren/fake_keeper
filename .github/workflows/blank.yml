name: Staging

on:
  push:
    branches: 
      - dev
  workflow_dispatch:
  pull_request:
#   workflows: ["E2E Test App"]
#   types:
#     - completed

jobs:
  containering:
    runs-on: ubuntu-latest

    steps:
      - run: echo "DONE"
